{
    "build": {
        "builder": "NIXPACKS",
        "buildCommand": "cd GymAssist && mvn clean package -DskipTests",
        "buildPath": "GymAssist"
    },
    "deploy": {
        "startCommand": "cd GymAssist && java -jar target/*.jar"
    }
}